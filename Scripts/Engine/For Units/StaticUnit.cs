using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[AddComponentMenu("Final Integrated Stuff/Static Game Unit")]
public class StaticUnit : MonoBehaviour {

	/*
	 * 
	 * A static unit is a unit that does not move, but still interacts with other units and the player somehow.
	 * Things like spike traps, turrets, etc...
	 * 
	 * 
	 */

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
